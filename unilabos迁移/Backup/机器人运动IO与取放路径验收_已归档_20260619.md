# 机器人运动、IO 与取放路径验收

> 状态：待现场执行。离线代码检查通过不等于硬件验收通过。

## 冻结事实

- 点表源：`机器人程序/机器人程序v0.11/roboprogram/point.json`
- 点数：86；SHA-256：`1f7bc0ad8d742933451d0f041b8f5d4fc62784cca32c3d60d60eef67727ad57c`
- 占位点：`P6/P24/P41/P42`
- 重复位姿：`P64/P65`、`P73/P74`，以及上述四个占位点
- 已验证点：当前仅 `P1/Home`，只允许 `move_j`
- IO 暂定事实：DO1 快换、DO3 吸盘、DO2+DO6 夹爪；DI1/DI2 为夹爪两端反馈，物理语义和 ON 极性待确认
- DO5/DO7/DO8 不进入首批生产协议

## 现场前置

- [ ] 确认 3400..3485 与现场其他任务无寄存器冲突
- [√] 将 `robot_mvp_minimal.lua` 合入机器人当前工程并在控制器侧完成语法检查
- [√] 低速、空载、单人控制，清空运动区域并确认急停可用
- [√] 记录机器人、工具、用户坐标系、点表版本和操作者

## 运动验收

- [√] `query`：不运动；pose/joint、last_action=24 正确
- [√] `home`：低速回 P1；last_action=26
- [√] `move_j`：已知安全点正常；不可达点返回 error=11；复位后回 FREE
- [√] `move_l`：从已知起点到已验收终点；不可达路径返回 error=14；复位后回 FREE
- [ ] 动作超时、TCP 断线、重连后不重复执行旧命令
- [ ] 同时发两条命令时，第二条被上位机串行锁等待，不发生寄存器交叉写

每个通过的点在 `UI-Upper/config/robot_point_overrides.json` 中增加
`status=validated` 和对应 `allowed_motion`，并记录标定时间；不要批量放开。

## IO 验收

逐项记录“命令 ID、DO 实际电平、机械动作、DI 电平、断电/急停安全态、超时结果”：

- [ ] 快换锁紧/释放（1/2）
- [ ] 吸盘开/关（3/4）
- [ ] 夹爪开/关（5/6）及 DO2+DO6 时序
- [ ] `get-state`（7）不改变任何输出
- [ ] 非白名单动作返回 error=15
- [ ] DI 反馈超时返回 error=16

只有确认 DI1/DI2 的物理含义与 ON 极性后，才把 Lua 中
`TOOL_DI_FEEDBACK_ENABLED` 改为 `true`；否则保持命令态反馈。

## 完整取放路径

先冻结一组 `ready / approach / target / retreat` 命名点，再按以下顺序低速验收：

1. `move_j(ready)`
2. `move_l(approach)`
3. `move_l(target)`
4. `tool_action(gripper-close | suction-on)`
5. `move_l(retreat)`
6. `move_j(ready)`

- [ ] 空载运行三次，无奇异位形、碰撞风险或异常绕行
- [ ] 带工件运行三次，抓取/释放与反馈一致
- [ ] 在 approach、target、工具动作后分别验证暂停/复位恢复策略
- [ ] 保存最终点位、速度、工具号、验收人、日期和结果
