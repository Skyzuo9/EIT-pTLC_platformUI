# 2026-05-11 修改记录

## Recipe / Queue / Debug 三模块交互与状态管理修复

1. **Recipe Tab 渲染修复**：`_render_params_form` 补齐 `string` / `select` 分支，修正 `ui.switch` 错误的 `label=` 关键字，并将当前编辑的 `RecipeTemplate` 挂到 `AppState.current_recipe` 供 Queue Tab 入队使用。
2. **配方保存/另存为修复**：`_save_as_recipe` 补上 `dialog.open()` 以触发弹窗显示；`_save_recipe` 改为以下拉框当前选中的配方名作为保存目标文件（而非 `recipe.name`），消除总写入 `standard_pTLC.yaml` 的 bug。
3. **Queue Tab 入队打通**：`RecipeStore` 新增 `save_tmp(recipe, sample_id)` 落盘 `recipes/.tmp/<sid>_<ts>.yaml`；Queue Tab 新增"<当前编辑中>"伪选项，入队时从 `state.current_recipe` 深拷贝并落盘临时快照。
4. **调度器懒启动与持久运行**：`ui/app.py` 注入 `state.scheduler_starter` 供 Queue Tab 懒启动；`_scheduler_background_task` 把 `run_until_empty()` 换成 `while not _aborted` 持久循环，队列空闲时 sleep 等待 UI 动态入队。
5. **`scheduler_running` 语义收窄**：从“后台任务存活”改为“有样品在执行/队列非空”，队列清空后 Debug Tab 的 collect 启动互锁自动解除。

## PLC 统一工位协议 v1.0 → v1.1（B + B2 方案）

- **修改目标**：采纳“工位独立 `_Busy` + 全局 BusyAggregator 聚合”（B）与“全工位推广业务命名，删除 Recipe_Param 抽象”（B2），解决多 PRG 对 `PLC_Busy` 写冲突与 PLC 端二次赋值冗余。
- **文档**：`docs/PLC_Unified_Stage_Protocol.md` v1.0→v1.1（§2 5变量→6变量、§4.2/4.3/4.4 collect 改业务直名 `collect_forward_instructions / collect_count`、§6 新增 v1.1 条目）。
- **上位机**：[plc_client.py](file:///e:/PHD/PKU/MoGroup/pTLC_platform/EIT_Project/UI-Upper/core/plc_client.py) NODE_TYPES 删 12 项 `Recipe_Param_*`、新增 collect 六件套与业务变量、新增 `_coerce_value` 重写 `send_recipe_params`；[base.py](file:///e:/PHD/PKU/MoGroup/pTLC_platform/EIT_Project/UI-Upper/core/stages/base.py) / [collect.py](file:///e:/PHD/PKU/MoGroup/pTLC_platform/EIT_Project/UI-Upper/core/stages/collect.py) `PARAM_CHANNEL_MAP` 改为 `{yaml_key: plc_var_name}`；[variable_monitor.py](file:///e:/PHD/PKU/MoGroup/pTLC_platform/EIT_Project/UI-Upper/core/variable_monitor.py) 监控列表同步。
- **Mock**：[mock/plc_server.py](file:///e:/PHD/PKU/MoGroup/pTLC_platform/EIT_Project/UI-Upper/mock/plc_server.py) `StageFSM` 新增 `busy_node` 派生 `collect_Busy`，新增 `_busy_aggregator_loop` 协程作为 `PLC_Busy` 唯一写者；`_snapshot_recipe_params` 改名 `_snapshot_collect_params`。
- **下游清理**：[debug.py](file:///e:/PHD/PKU/MoGroup/pTLC_platform/EIT_Project/UI-Upper/ui/sections/debug.py) Jog 区删除 Recipe 参数面板（12 通道输入 + `_FLOAT_CHANNELS/_INT_CHANNELS/_ALL_CHANNELS/_collect_params/param_inputs`），仅保留 ActionID 触发；工位级业务参数走 Phase A 调试区。
- **PLC 实机 ST**：[plc_collection.txt](file:///e:/PHD/PKU/MoGroup/pTLC_platform/EIT_Project/PLCsoftware/OPCUAtest/plc_collection.txt) 删除 `ton3s/ton5s/bOutput/bEnable/bStepEnable/bErrorReset/MODE_State` mock 残留与此处的 `PLC_Busy` 赋值；改为 `collect_Enable / collect_Reset` 电平驱动 + `stepTimer` 局部 TON + `PLC_EStop` 急停；新增 BusyAggregator PRG 模板。
- **验证**：`python -c 'import ast ...'` 5 个文件语法 OK；grep `RECIPE_*_CHANNELS | Recipe_Param_ | Gcode_Name` 0 残留；`from ui.sections import debug` 导入 OK；`import mock.plc_server` 默认 22 变量、含 `collect_Busy / collect_forward_instructions`、不含 `Recipe_Param_A`。
- **已知问题**：CODESYS 实机侧未应用新 ST（需新增 GVL `collect_Enable/Reset/Busy + PLC_EStop`、新建 BusyAggregator PRG、OPC UA Symbol 暴露、Task 调度）；step 20 真实注射泵 FB 调用仍为占位 FOR 循环。
- **后续计划**：(a) mock 端到端冒烟 main.py + CollectStage；(b) CODESYS 完成 GVL／Task／OPC UA 配置后在实机跑空转 collect；(c) step 20 接入真实注射泵 FB。
