# 2026-05-29 实验记录

## 修改目标
实现耗材管理系统 Phase 1：ConsumableManager 纯上位机核心模块，为后续 Phase 2（UI）和 Phase 3（PLC 接口）提供稳定的数据模型与决策抽象层。

## 核心改动
- **新增 `core/consumable_manager.py`**（~370 行）：枚举（SlotStatus 三态 / ConsumableType / StagingId）+ 数据类（StagedPlate / RackSlot / ConsumableSnapshot）+ 异常层级 + ConsumableManager 决策类
- **新增 `tests/test_consumable_manager.py`**（~300 行）：16 个测试用例覆盖全部决策路径、异常路径、并发安全
- **实施修正 4 项**：R1 并发安全（take 在 prepare 内原子执行）、R2 FILLED 阻塞优先检查、R3 合并 manual_reset_staging（奥卡姆剃刀）、R4 SolventChannel 延后（正交关注点分离）

## 实验结果
- 16 tests in 0.071s — **全部通过**（Python 3.13）
- ConsumableManager 可独立实例化，无 PLC/UI/Database 依赖

## 已知问题
- `plans/` 目录在 `.gitignore` 中，计划文档不纳入版本控制
- SolventChannel 溶剂管理推迟到后续 Phase 按需添加

## 后续计划
- Phase 2（UI）：独立耗材 Tab + Dashboard 摘要卡片，可立即启动
- Phase 3（PLC 接口）：OPC UA 变量定义 + Mock 适配，需 PLC 工程师确认后可并行
- Phase 4（Stage 集成）：ScrapeStage/CollectStage 调用 prepare_for_scrape/collect

## 状态
**待审阅**
