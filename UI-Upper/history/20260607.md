# 2026-06-07 实验日志

## 修改目标
基于本轮代码审阅（11 项 finding，归并为 6 任务）从第一性原理 + 奥卡姆剃刀实施修复，重点解决 Vision↔DB 契约断裂（Critical #1）、耗材数字化收尾、CLI 模式数据隔离、工艺默认值漂移、PLC v1.10 协议文档化、Recovery 误操作防护。

## 核心改动
- **T2 耗材数字化收尾**：测试套全面迁移到数字显示编号 1-6（粉/玻各 6），`prepare_for_scrape/collect` 入口加 `isinstance(plate_id, int)` assert 杜绝旧字母板号回流，UI 文案改 "粉3"/"玻1" 形式。
- **T3 CLI 内存版**：`collect` / 终端 `pltc` 模式 ConsumableManager 改 `persistence_path=None`，压测调度不再污染 `data/consumable_state.json` 真账本。
- **T4 收集通道 schema 放宽**：`solvent_channel` min=1, max=4（硬件支持多通道，现工艺默认 2），不再锁死 [2,2]。
- **T5 PLC v1.10 提示**：心跳 KeyError 降级 warning 文案附加 "PLC 工程是否已升级到 v1.10?"，AGENTS.md 加 Scrape FSM 子步语义版本对照表。
- **T6 Recovery 二次确认**：`_do_demote` 新增 dialog 二次确认（手动降级 NEEDS_DRAIN 不可逆），AGENTS.md 加 Handoff Orphan Diagnosis（§②½）章节。
- **T1.1 DB ScrapeArrays 表**：新建 `scrape_arrays(sample_id, band_id, ...)` 表 + `upsert_scrape_arrays/get_scrape_arrays` 方法，`SampleStore.trigger_db_scrape_arrays()` sideband。
- **T1.2 写入触发**：`ScrapeStage._persist_scrape_arrays_artifact` helper（渲染 PNG + DB sideband），两条 `_generate_gcode*` 路径成功后调用；Vision Tab `_on_generate_gcode` 调 `trigger_db_scrape_arrays`，state 双字段（`vision_arrays_summary` / `vision_preview_path`）同步赋值。
- **T1.3 History 详情页 v2 路径**：优先读 `scrape_arrays`，为空回退 `gcode_files`（存量样品兼容）。

## 实验结果
- 12 个改动文件 `py_compile` 全部通过。
- `tests.test_consumable_manager` 43 测试全过（修复 2 个 internal_num 残留断言）。
- 8 个 commit 拆分提交，单 commit 单语义可独立 revert。

## 已知问题
- **`gcode_files` 表保留待评估**：本轮 v2 主路径迁移到 `scrape_arrays` 表后，`gcode_files` 仅存量样品 + `scripts/ingest_samples_to_db.py` 仍写入。需后续决策：(a) 完全废弃并迁数据；(b) 保留为历史归档；(c) 保留双轨直至 ingest 脚本同步迁移。
- 旧 memory `collect溶剂通道参数UI显示修正方案` 已更新为 schema 范围 [1,4]，与 T4 决策对齐。
- 工作树仍有较多前置 baseline 改动（ScrapeArrays 协议主体 / 数字编号迁移 / 配方文件等），未在本轮 8 个 commit 范围内，由后续按主题单独处理。

## 后续计划
- 决策 `gcode_files` 表去留并实施（建议结合 ingest 脚本迁移一次性完成）。
- 真机验证 ScrapeArrays 协议 + DB 同步链端到端：样品执行 → DB 落库 → History Tab 详情显示 PNG + 关键标量。
- 余下 baseline 改动（mock/plc_server.py、core/scheduler.py、recipes/* 等）按主题打包后续 commit。
- 验证 Recovery Tab 二次确认对话框在 NiceGUI 异步事件 handler 下 slot context 表现。
