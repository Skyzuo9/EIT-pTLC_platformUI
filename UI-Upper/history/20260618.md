# 2026-06-18 实验日志

## 修改目标

冻结当前上位机程序为可追溯基线，在不改动原 `parallel` 工作区及其已暂存机器人实验的前提下，为新一代上位机建立独立 Git worktree。

## 核心变更

- 以提交 `350f6d1` 为程序起点，建立 `codex/ui-upper-baseline` 基线分支。
- 纳入当前运行所需但原先未跟踪的配方：`All_Robo.yaml`、`collect_only.yaml`、`scarpeandcollect_pTLC.yaml` 和 `standard_pTLC.yaml`。
- 纳入大恒相机调用封装 `scripts/daheng_capture.py`；厂商 `gxipy` SDK 仍作为本地外部依赖。
- 完善忽略规则，排除缓存、日志、SQLite、样品输出、临时测试文件和本地厂商 SDK，仅保留 Mock 默认图像 fixture。
- 从版本库移除已被误跟踪的 `__pycache__/*.pyc`。

## 实验结果

- 基线内容与原工作区物理隔离，原工作区的源码、暂存区和未跟踪实验文件均未被改写。
- YAML 配方解析、Python 语法和 Mock 启动级别检查在基线提交前执行。
- 新上位机将从该基线创建 `codex/ui-upper-next` 独立 worktree。

## 已知问题

- 大恒 `gxipy` SDK 目录未纳入 Git；新 worktree 若连接真实相机，需按厂商授权和现场环境单独安装。
- 原工作区中正在进行的机器人 MVP 实验未并入稳定基线，后续应审核后选择性迁移。
- 基线验证不包含真实 PLC、机器人、相机或泵的现场运动测试。

## 后续计划

- 在新 worktree 中编写下一代上位机，原工作区仅用于当前稳定版回溯和现场维护。
- 新程序默认使用 Mock PLC；在单写者边界未验证前，不与当前上位机同时连接真实 PLC。
