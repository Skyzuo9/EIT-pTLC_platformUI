# 20260603 实验记录（第二次提交）

## 修改目标
CNC 点位容量升级满足精密刮取需求；耗材决策闭环集成到 Scrape/Collect 执行流程；液位监测缸视觉弹窗增加标定/ROI 管理完整交互；修正上样空气隔离段参数语义。

## 核心改动
1. **CNC 点位数组 200→400**：`cnc_path_generator` / `config.py` / `mock/plc_server` / `CNCTest/scrape_template_v2.cnc` 全链路升级，overlap_ratio 默认 0.3→0.4 提升覆盖率
2. **耗材决策 v2.5 集成 ScrapeStage/CollectStage**：`prepare_for_scrape` / `get_powder_return_slot` / `prepare_for_collect` / `on_collect_done` 全流程闭环；新增 `collect_Powder_Return_Slot` PLC 变量；RecipeTask→Scheduler 注入链完成
3. **tank_vision_dialog 大幅扩展**：标定启动/停止 + ROI 设置/清除 + 配置重载 + 恢复出厂完整 UI；回调生命周期管理（注册/移除避免累积）
4. **WaterLevelClient**：新增 7 个 MQTT CMD topic + `remove_on_data/remove_on_ack` 回调清理方法
5. **参数重命名 `margin_volume_ml` → `air_buffer_ml`**：语义更精确——表示空气隔离段而非余量，默认值 1.0→3.0 mL
6. **Consumable Tab DI 读取**：改用 `read_consumable_sensor()`（IX11/IX12 BYTE 解码），消除旧 `Consumable_Sensor` 数组路径依赖

## 实验结果
- commit c5da8cc 涵盖 30 个文件，+1232 / -452 行
- 删除 3 个过时 recipe（collect_only / standard_pTLC / standard_pTLC2）
- mock 同构更新，本地可跑通全路径

## 已知问题
- tank_vision_dialog 标定超时逻辑依赖香橙派 MQTT ack 稳定性，弱网环境未测试
- `get_powder_return_slot` 在 scrape 未走耗材决策的遗留路径下返回 0 → CollectStage raise RuntimeError（符合设计意图，但需确保旧 recipe 不会触发）

## 后续计划
- 真机联调：验证 400 点数组 PLC 侧 CNC 模板解析 + 耗材动作码执行
- 液位标定 UI 完成后做多通道并行标定压测
- 清理 `__pycache__` 中二进制 pyc 未跟踪提交问题
