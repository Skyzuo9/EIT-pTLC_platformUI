# 2026-05-16 修改记录

## Recipe 配方编辑 Tab 临时配方占位符修复

1. **修改目标**：修复配方界面下拉选单默认选中实际配方名而非临时占位符的 bug——"<当前编辑>"应作为默认暂存状态，保存时应走"另存为"流程，避免误覆盖现有配方。
2. **核心改动**（1 文件，26+/9-）：
   - 新增模块常量 `_EDITING_PLACEHOLDER = "<当前编辑>"`，统一管理临时配方占位符名称
   - `recipe_load_bar()`：下拉选项列表头部插入"<当前编辑>"，默认选中逻辑改为仅在已选中实际配方名时沿用，否则默认显示占位符
   - `_load_recipe()`：拦截选中"<当前编辑>"时点击"加载"的操作，提示"请选择一个实际配方"
   - `_new_recipe()`：设置 `selected_name` 为占位符，刷新 `recipe_load_bar` 使下拉框同步，提示改为"已创建默认配方（未保存）"
   - `_save_recipe()`：检测到 `target_name == "<当前编辑>"` 时自动调用 `_save_as_recipe()`，走另存为流程
   - `_do_save_as()`：拒绝使用保留名称"<当前编辑>"作为新配方名
3. **实验结果**：commit 4bd0002，下拉选单默认显示"<当前编辑>"，新建配方处于临时状态，保存时自动弹出另存为对话框
4. **已知问题**：无
5. **后续计划**：无

---

## CameraService 抽象层 + ScrapeStage 双帧视觉集成 + main.py 统一入口清理

1. **修改目标**：为双帧视觉方案铺路——在 ScrapeStage Step 20 集成相机拍照，拍照后自动触发 `analyze_full()` 并推送结果到 Vision Tab；同时清理 main.py 中旧 Batch 2/3/4/collect-only 入口，统一为 `--config` 驱动模式。
2. **核心改动**（18 文件，769+/556-）：
   - 新增 `core/camera_service.py`：`CameraService` ABC → `MockCameraService`（复制预置测试图）/ `DahengCameraService`（PALLAS SDK 预留，NotImplementedError）
   - `ScrapeStage`：Step 20 到达后调用 `camera.capture()` → `vision.analyze_full()` → `_push_to_vision_tab()`，自动推导 `before_path`/`after_path`（SampleStore → 文件系统回退）；新增 `_analyze_vision_full()` 和 `_push_to_vision_tab()` 方法
   - `VisionService`：新增 `_generate_annotated_image()` 基于 PIL 在 after.jpg 上绘制 band 矩形框+标签；`_extract_band_infos()` 改进 bbox_cm 计算（从 centroid + width/span 推导）
   - Vision Tab：新增"拍照结果" tab 显示 ScrapeStage 推送的 after.jpg；ScrapeStage 占用守卫（禁用手动分析按钮+样品下拉）；`set_classes()` → `classes(replace=...)` 修复
   - `main.py`：移除 `--batch2/3/4`、`--collect-only`、`--real` 等旧入口及关联函数，仅保留 `--config` 路径
   - 配置层：新增 `CameraCfg` dataclass（mock/test_image/test_before_image），`config.example.yaml` 新增 camera 段（默认注释）
   - `SampleStore`：新增 `get_before_path(sample_id)` 和 `get_sample_dir(sample_id)` 方法
   - `AppState`：新增 `vision_scrape_after_path` 字段；`mock_vision` 默认改为 `False`
   - `RecipeTask` / `SampleTask`：`camera` 参数透传至 `ScrapeStage`
   - AGENTS.md：更新 System Topology（添加 camera_service）、Key Modules 表格、ScrapeStage 描述
3. **实验结果**：commit dce3ad4。代码结构完整，但**未经端到端测试验证**——ScrapeStep20→拍照→视觉分析→Vision Tab推送的完整链路尚未在 mock 环境下实际走通
4. **已知问题**：
   - ScrapeStage `_push_to_vision_tab()` 中 `vision_scrape_waiting` 设置与 `_wait_for_band_selection_and_generate_gcode()` 中存在潜在双重设置，需验证是否冲突
   - Vision Tab `refresh_vision()` 刷新逻辑在 ScrapeStage 占用时可能存在时序竞争
   - `DahengCameraService.capture()` 未实现，双帧视觉实机集成待后续完成
5. **后续计划**：
   - 双帧视觉方案推进：before.jpg 来源（spotting 阶段拍照或 MockCameraService.test_before_image）
   - 端到端测试：mock 环境下 ScrapeStage 完整链路验证
   - DahengCameraService 实机 SDK 集成

---

## 7 计划文档审阅 + Scrape v1.6 PhotoMode 路由 + 审阅修复

1. **修改目标**：审阅 7 个计划文档的实施完成度，并修复审阅中发现的遗漏项（ScrapeStage 视觉跳过分支缺安全占位 G-code、Vision Tab ScrapeStage 占用守卫、拍照失败后 after_path 清空、_wait_fsm_idle 强制 Reset 兜底等）；同时完成 PLC 刮板协议 v1.5→v1.6 升级（PhotoMode 路由 + Step 15 before-photo 乒乓）。
2. **核心改动**（11 文件，357+/126-，commit 904a56f）：
   - `PLCsoftware/OPCUAtest/plc_scraping.txt`：协议 v1.5→v1.6，新增 `scrape_PhotoMode`(INT) + Step 15(before-photo乒乓) + Phase A门控(phaseA_done_15/20) + Step 10 PhotoMode路由 + 消费型变量归零修正
   - `UI-Upper/core/stages/scrape.py`：`SCRAPE_SUB_STEPS` 新增 Step 15 + `STEP` 常量；视觉跳过分支补写 `SAFE_PLACEHOLDER_GCODE`；拍照失败后 `self._after_path = None`；`_wait_fsm_idle` 超时后尝试强制 Reset；Vision Tab 确认超时 `future.cancel()`
   - `UI-Upper/core/camera_service.py`：`capture()` 新增 `filename` 参数（默认 `after.jpg`），支持双帧拍照 `before.jpg`
   - `UI-Upper/core/plc_client.py`：`NODE_TYPES` 新增 `scrape_PhotoMode: Int16`
   - `UI-Upper/mock/plc_server.py`：注册 `scrape_PhotoMode` 变量；`_run_sequence` 改按索引迭代支持 step_hook 动态修改 trace；`_scrape_step10_hook` PhotoMode 路由；`confirm_steps` 新增 Step 15
   - `UI-Upper/ui/sections/vision.py`：ScrapeStage 占用守卫（禁用手动分析+样品下拉）；`_on_confirm_gcode` 无等待时 `ui.notify` 提示；`vision_scrape_waiting` 单写者原则
   - `UI-Upper/ui/app.py`：Vision Tab dirty check 新增 `scrape_waiting` + `scrape_after_path`
   - `UI-Upper/scripts/sample_pump_translator.py`：注释翻译 + 格式整理
   - `docs/PLC_Unified_Stage_Protocol.md`：升级至 v1.6，新增 PhotoMode 路由说明 + 路径 B(before-photo)时序图
   - `AGENTS.md`：更新 ScrapeStage 描述 + CameraService 双帧 + `Path.is_file()` 陷阱
3. **实验结果**：commit 904a56f。**未经端到端验证**——所有变更仅基于代码审阅和逻辑推理，未在 mock 环境下实际运行验证。
4. **7 计划文档审阅总结**：

   | 计划文档 | 状态 | 备注 |
   |----------|------|------|
   | 双帧视觉分析修复与入口精简 | ✅ 已完成 | Phase 1（vision_service is_file 验证+annotate PIL）+ Phase 2（main.py 入口精简）均已完成 |
   | Scrape审阅修复计划 | ✅ 已完成 | 12 项任务全部完成：PLC ST 修复、ScrapeStage 改进、confirm_callback 适配、SAFE_PLACEHOLDER_GCODE、_wait_fsm_idle 强制 Reset、STEP 常量等 |
   | Vision_Tab_拍照刮板可视化 | ✅ 已完成 | 7 项任务全部完成：VisionService API、GCodeGenerator、GCodeRenderer、Vision Tab UI、ScrapeStage→Vision Tab 桥接、照片显示、ScrapeStage 占用守卫 |
   | Vision_Tab图像显示第一性修复 | ✅ 已完成 | 6 项任务全部完成：移除 matplotlib mock、PIL annotated image、BandInfo bbox_cm、_extract_band_infos 改进、mock 路径返回 None、vision_result 结构完善 |
   | Scrape拍照-Vision_Tab集成 | ✅ 已完成 | 4 项任务全部完成：ScrapeStage→Vision Tab 桥接、analyze_full 调用、band 选择 Future、G-code 下发 |
   | Scrape_v1.5_审阅修复计划 | ✅ 已完成 | 14 项任务全部完成：confirm_callback、_scrape_lock、debug.py、CancelledError、_emit_stage_start 方案A、vision_scrape_waiting 单写者等 |
   | **双帧视觉BeforePhoto实施计划** | **⚠️ 部分完成** | PLC 侧基础设施已就绪（plc_scraping.txt Step 15 + PhotoMode 路由、mock server、NODE_TYPES），但上位机侧 Phase 1 核心项未实现 |

5. **未完成项（BeforePhoto 实施计划 Phase 1）**：
   - `core/stages/before_photo.py`：BeforePhotoStage 类尚未创建（需共享 `_scrape_lock`，PhotoMode=1 路径）
   - `core/stages/__init__.py`：`STAGE_ORDER` 和 `STAGE_REGISTRY` 尚未注册 `before_photo`
   - `core/sample_store.py`：`set_before_path()` 方法尚未添加
   - `core/variable_monitor.py`：`scrape_PhotoMode` 尚未加入 `MONITORED_VARS`
   - 配方 YAML：尚未添加 `before_photo` 阶段
   - `core/task.py`：RecipeTask 尚未注入 camera/sample_store 给 before_photo 阶段
6. **已知问题**：
   - 所有变更未经端到端验证，ScrapeStage 完整链路（拍照→视觉→Vision Tab→G-code→Confirm）未实际走通
   - `_push_to_vision_tab()` 与 `_wait_for_band_selection_and_generate_gcode()` 中 `vision_scrape_waiting` 设置是否存在双重设置冲突，待验证
   - Vision Tab `refresh_vision()` 在 ScrapeStage 占用时可能存在时序竞争
7. **后续计划**：
   - 端到端 mock 验证：ScrapeStage 完整链路实际运行
   - BeforePhotoStage Phase 1 实施（上位机侧 6 项未完成）
   - DahengCameraService 实机 SDK 集成

---

## BeforePhotoStage 全链路实现 + Vision Tab Band 复选框自动 G-code 预览 + ScrapeStage 防御性 PhotoMode 归零

1. **修改目标**：实现双帧视觉 BeforePhotoStage 上位机侧 Phase 1 全部 6 项未完成项——BeforePhotoStage 类创建、STAGE_ORDER/REGISTRY 注册、SampleStore.set_before_path()、variable_monitor 新增 scrape_PhotoMode、配方 YAML 添加 before_photo 阶段、RecipeTask 注入 camera/sample_store；同时修复 Vision Tab Band 复选框交互（勾选后自动重新生成 G-code 预览）、VisionService plate_bbox_px dict/list 格式兼容、ScrapeStage 防御性 PhotoMode=0 写入、mock PLC PhotoMode 消费语义归零等。
2. **核心改动**（20 文件，383+/44-，commit e1e4b8d）：
   - 新增 `core/stages/before_photo.py`：`BeforePhotoStage` 类，复用 scrape FSM（PhotoMode=1 路由到 Step 15 before-photo 乒乓），共享 `_scrape_lock` 硬件互斥；拍照后推送到 Vision Tab（`vision_before_photo_path`）+ 存入 SampleStore（`set_before_path()`）
   - `core/stages/__init__.py`：`STAGE_ORDER` 新增 `before_photo`（spotting → before_photo → develop → scrape → collect）；`STAGE_REGISTRY` 注册；锁共享赋值 `BeforePhotoStage._scrape_lock = ScrapeStage._scrape_lock`
   - `core/stages/scrape.py`：防御性写 `scrape_PhotoMode=0` 防止 BeforePhotoStage 残留 PhotoMode=1 导致错误路由；拍照后推送 after 照片到 Vision Tab（`vision_scrape_after_path`）
   - `core/camera_service.py`：`MockCameraService.capture()` 改进——`capture(filename="before.jpg")` 使用 `_test_before_image` 源；自动 before 复制仅在 `filename="after.jpg"` 时触发（避免 BeforePhotoStage 显式调用 `capture("before.jpg")` 时重复覆盖）
   - `core/sample_store.py`：新增 `set_before_path(sample_id, path)` 方法——将图像复制到样品目录下的 before.jpg
   - `core/vision_service.py`：`_extract_band_infos()` 中 `plate_bbox_px` 兼容 dict 格式 `{"x","y","w","h"}` 和 list 格式 `[x1,y1,x2,y2]`，防止外部数据格式不一致时崩溃
   - `core/scheduler.py`：`Scheduler.__init__` 新增 `sample_store` 参数，透传给 `RecipeTask`
   - `core/task.py`：`RecipeTask.__init__` 新增 `sample_store` 参数；`_build_executor` 为 `BeforePhotoStage` 注入 camera + sample_store，为 `ScrapeStage` 注入 sample_store
   - `core/variable_monitor.py`：`MONITORED_VARS` 新增 `scrape_PhotoMode`
   - `core/stage_state_registry.py`：默认 stages 列表新增 `before_photo`
   - `mock/plc_server.py`：`_scrape_step10_hook` 实现 PhotoMode 消费语义——路由完成后将 `scrape_PhotoMode` 归零（显式 `VariantType.Int16`），防止下一次 scrape 启动时残留值导致错误路由
   - `ui/sections/vision.py`：BeforePhotoStage 占用守卫（`vision_before_photo_waiting`）+ before 照片显示；Band 复选框变化时自动调用 `_regenerate_gcode_preview()` 重新生成 G-code 文本+路径图；占用指示器区分 Scrape/BeforePhoto 场景
   - `ui/state.py`：新增 `vision_before_photo_waiting`（bool）和 `vision_before_photo_path`（Path）字段
   - `ui/sections/flow.py`：before_photo 颜色映射（cyan）、时间线步骤 `[10, 15]`、工位卡片支持；scrape 时间线新增 Step 15
   - `ui/app.py`：SampleStore 初始化逻辑；Scheduler 注入 sample_store；Vision Tab dirty check 新增 `before_photo_waiting` + `before_photo_path`
   - 配方 YAML：`standard_pTLC.yaml`、`standard_pTLC2.yaml`、`collect_only.yaml` 新增 `before_photo` 阶段条目
   - `config.example.yaml`：camera 段默认启用（mock: true），双帧参数取消注释
3. **实验结果**：commit e1e4b8d。**未经端到端验证**——BeforePhotoStage 完整链路（写 PhotoMode=1 → start_stage → await Step 15 → 拍照 → Confirm → Done）尚未在 mock 环境下实际运行走通。
4. **已知问题**：
   - BeforePhotoStage 与 ScrapeStage 的 `_scrape_lock` 共享赋值在 `__init__.py` 中完成，但 `BeforePhotoStage` 类定义中 `_scrape_lock = None` 占位——如果 `BeforePhotoStage` 在 `ScrapeStage` 之前被单独实例化（理论上不会），锁仍为 None
   - Vision Tab `_regenerate_gcode_preview()` 同步调用 GCodeGenerator，如果生成耗时较长可能阻塞 UI 线程
   - `DahengCameraService.capture()` 未实现
5. **后续计划**：
   - 端到端 mock 验证：BeforePhotoStage + ScrapeStage 完整链路实际运行
   - DahengCameraService 实机 SDK 集成
   - Vision Tab `_regenerate_gcode_preview` 异步化（如需要）
   - 双帧视觉完整端到端测试
